import React from 'react';
import NavBar from '../navBar/navBar';

const NotFound = () => {
    return (
        <div>
            <NavBar />
            <h2>No encontrado :(</h2>
        </div>
    );
};

export default NotFound;